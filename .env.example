# Copy this file to .env and change the values before running the backend server.
PORT=4000
APP_SECRET=change-this-secret

# Optional: default admin user for the local backend
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=changeMe123!
